# Copy to .env for local dev (or set these in Render/Railway dashboard).
PORT=8787

# Optional — only needed to bridge YouTube Live chat votes / Super Chats.
# YouTube Data API v3 key (Google Cloud console).
YOUTUBE_API_KEY=
